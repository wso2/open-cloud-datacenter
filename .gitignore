# Binaries
dc-api/dc-api
dc-api/webhook
dcctl/dcctl
*.exe

# Test debris — local-only run logs / debug notes from integration runs
dc-api/test-output/

# Go build artifacts
*.test
*.out

# Credentials & secrets — NEVER commit these
*.secret.*
secrets.yaml
.env
.env.*
!.env.example
dc-api/deploy/secret.yaml
credentials.json
kubeconfig
*.kubeconfig

# dcctl local state
~/.dcctl/

# Local dev-loop overrides (per-developer cluster context/namespace/hostnames)
scripts/dev-up.env

# Terraform (if any TF files are added)
*.tfstate
*.tfstate.backup
.terraform/
*.tfvars
!*.tfvars.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store
__MACOSX/

# Go module cache (not needed in repo)
vendor/

# Log files
*.log

# Claude Code runtime artefacts (local-only state)
.claude/scheduled_tasks.lock

# Pandoc-rendered .docx exports — internal sharing only, not source of truth
docs/*.docx

# Local-only scratch / personal notes — never committed
.misc/

# Hypothesis example DB cache (produced by the schemathesis contract test)
dc-api/.hypothesis/