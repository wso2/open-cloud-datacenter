apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service.yaml
  - statefulset.yaml
# dc-postgres-secret is a SealedSecret in the consumer overlay
# (generated by scripts/init-flux.sh --seal-secrets).
