namespace: registry-system

namePrefix: regi-

resources:
- ../crd
- ../rbac
- ../manager
- metrics_service.yaml

# Override the operator image here (or run
# `kustomize edit set image controller=<repo>/<image>:<tag>`).
images:
- name: controller
  newName: ghcr.io/sandaruwijewardhana/registry-provisioner
  newTag: latest
